<form method="post" action="{{ path('app_category_delete', {'idCateg': category.idCateg}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.idCateg) }}">
    <button class="btn">Delete</button>
</form>
