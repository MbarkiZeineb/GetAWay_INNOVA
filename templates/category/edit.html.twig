{% extends 'base_back.html.twig' %}


{% block content %}
    <h1>Edit Category</h1>
    <br>
    <br>
    <br>
    <br>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-default" href="{{ path('app_category_index') }}">back to list</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
