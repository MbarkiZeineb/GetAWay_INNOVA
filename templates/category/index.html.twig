{% extends 'base_front.html.twig' %}


{% block content %}
    <br>
    <br>
    <br>
    <br>
    <h1>Category index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdCateg</th>
                <th>NomCateg</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.idCateg }}</td>
                <td>{{ category.nomCateg }}</td>
                <td>
                    <a href="{{ path('app_category_show', {'idCateg': category.idCateg}) }}">show</a>
                    <a href="{{ path('app_category_edit', {'idCateg': category.idCateg}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_category_new') }}">Create new</a>
{% endblock %}
