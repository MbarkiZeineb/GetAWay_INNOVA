<form method="post" action="{{ path('app_promostion_delete', {'idRef': promostion.idRef}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promostion.idRef) }}">
    <button class="btn">Delete</button>
</form>
