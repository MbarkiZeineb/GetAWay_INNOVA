{% extends 'base_back.html.twig' %}

{% block title %}Avis{% endblock %}

{% block content %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Refavis</th>
                <td>{{ avi.refavis }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ avi.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ avi.date ? avi.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ avi.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_index') }}">back to list</a>

    <a href="{{ path('app_avis_edit', {'refavis': avi.refavis}) }}">edit</a>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
