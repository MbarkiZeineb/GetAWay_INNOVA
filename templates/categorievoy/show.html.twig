
{% extends 'base_back.html.twig' %}

{% block content %}
    <h1>Categorievoy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcat</th>
                <td>{{ categorievoy.idcat }}</td>
            </tr>
            <tr>
                <th>Nomcat</th>
                <td>{{ categorievoy.nomcat }}</td>
            </tr>
            <tr>
                <th>Capacitevoy</th>
                <td>{{ categorievoy.capacitevoy }}</td>
            </tr>
            <tr>
                <th>Exigence</th>
                <td>{{ categorievoy.exigence }}</td>
            </tr>
            <tr>
                <th>Reduction</th>
                <td>{{ categorievoy.reduction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorievoy_index') }}">back to list</a>

    <a href="{{ path('app_categorievoy_edit', {'idcat': categorievoy.idcat}) }}">edit</a>

    {{ include('categorievoy/_delete_form.html.twig') }}
{% endblock %}
