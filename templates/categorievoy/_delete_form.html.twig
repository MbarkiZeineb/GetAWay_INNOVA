<form method="post" action="{{ path('app_categorievoy_delete', {'idcat': categorievoy.idcat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorievoy.idcat) }}">
    <button class="btn">Delete</button>
</form>
