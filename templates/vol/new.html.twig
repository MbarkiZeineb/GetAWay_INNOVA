{% extends 'back_agent.html.twig' %}



{% block content %}
    <br>
    <br>
    <br>
    <br>
    <h1>Create new Vol :</h1>
    {% for message in app.flashes('warning') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    {{ include('vol/_form.html.twig') }}

    <a href="{{ path('app_vol',{'ida':app.user.id}) }}">back to list</a>
{% endblock %}
