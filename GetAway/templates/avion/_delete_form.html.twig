<form method="post" action="{{ path('app_avion_delete', {'idAvion': avion.idAvion}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avion.idAvion) }}">
    <button class="btn">Delete</button>
</form>
