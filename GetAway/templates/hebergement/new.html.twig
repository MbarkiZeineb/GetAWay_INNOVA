<!DOCTYPE html>

<title>New Hebergement</title>

{% block body %}
    <h1>Create new Hebergement</h1>

    {{ include('hebergement/_form.html.twig') }}

    <a href="{{ path('app_hebergement_index') }}">back to list</a>
{% endblock %}
