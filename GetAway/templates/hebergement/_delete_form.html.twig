<form method="post" action="{{ path('app_hebergement_delete', {'referance': hebergement.referance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.referance) }}">
    <button class="btn">Delete</button>
</form>
