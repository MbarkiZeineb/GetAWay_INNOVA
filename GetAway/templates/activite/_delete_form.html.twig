<form method="post" action="{{ path('app_activite_delete', {'refact': activite.refact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.refact) }}">
    <button class="btn">Delete</button>
</form>
